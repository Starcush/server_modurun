import userUrl from '../tracks/userUrl';

export default {
  track: {
    post: userUrl.post,
    delete: userUrl.delete,
    patch: userUrl.patch,
    get: userUrl.get,
    postRate: userUrl.postRate,
  },
};
